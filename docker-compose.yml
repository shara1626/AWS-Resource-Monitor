services:
  aws-monitor:
    build: .
    container_name: aws-resource-monitor

    ports:
      - "3000:3000"

    env_file:
      - .env
    
    volumes:
      - .:/app

    restart: unless-stopped
